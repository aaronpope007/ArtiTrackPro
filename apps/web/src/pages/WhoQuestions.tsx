import { Box, Typography, Card, CardContent } from '@mui/material';

export default function WhoQuestions() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Who Questions
      </Typography>
      <Card>
        <CardContent>
          <Typography variant="body1">
            This page will contain "who" question exercises and practice activities.
          </Typography>
        </CardContent>
      </Card>
    </Box>
  );
}
